# CLAUDE.md

本文件定义 DataFlow Inspector 仓库内的工程约束、运行边界、修改原则与交付要求。

适用范围：本仓库根目录及所有子目录。

## 1. 项目定位

DataFlow Inspector 是面向 DWS / 数仓分层加工场景的分析产品，不是纯演示页面。

当前交付基线是 P2 可用版本，服务于约 5 人的可信内部小组。P3 权限、审计、规模化与生产治理已延期到 `docs/future-roadmap.md`，不得把这些未来事项误判为当前功能修复的前置条件。

核心目标：

- 导入 DDL、ETL SQL、作业清单、脱敏样例
- 解析表、字段、指标、作业流、表级与字段级血缘
- 识别时间口径、`SELECT *`、指标过滤漂移等风险
- 为变更提供版本比较与上下游影响分析

禁止把静态示意内容伪装成真实分析结果。

## 2. 运行边界

### 2.1 端口规则

- 绝对不要占用、停止、重启 `127.0.0.1:8080`
- 本仓库本地自测默认使用：
  - 后端：`127.0.0.1:18080`
  - 前端：`127.0.0.1:15173`

### 2.2 启动方式

优先使用仓库已有方式：

- `make check`
- `make dev`
- `make stop`
- `make smoke`

如需手工启动：

- 后端工作目录：`backend/`
- 前端工作目录：`frontend/src/`

### 2.3 数据与安全边界

- 不连接真实生产数据库
- 不执行用户上传 SQL
- 仅做静态解析与只读分析
- 不把未验证的推断血缘当作“已确认事实”

## 3. 代码结构职责

### 3.1 后端

- `backend/app/main.py`
  - 应用工厂与路由装配
  - 中间件、生命周期和统一异常处理

- `backend/app/api/`
  - HTTP 输入输出与状态码
  - 调用 service，不承载解析和持久化细节

- `backend/app/services/`
  - 导入、查询、比较、影响分析、元数据和助手领域编排

- `backend/app/db/`
  - 数据库连接、schema 版本和 repository

- `backend/app/tasks/`
  - 异步导入队列、任务认领和启动恢复

- `backend/app/parser/`
  - `ddl_parser.py`：DDL 解析
  - `sql_parser.py`：SQL / 血缘解析
  - `analyzer.py`：整合分析流程
  - `regex_fallback.py`：降级兜底
  - `dialect_dws.py`：DWS 方言特性

原则：

- `main.py` 只做组合，不得重新成为业务中心
- API 层不得直接拼装复杂 SQL 或复制 service 逻辑
- 解析逻辑放 `parser/`，业务编排放 `services/`，持久化放 repository
- 异步任务必须原子认领；进程重启后能够恢复待处理任务
- ZIP 必须先完整校验再创建导入记录，拒绝路径穿越和压缩炸弹

### 3.2 前端

- `frontend/src/index.html`
  - 应用挂载点与样式入口

- `frontend/src/app.js`
  - 应用组合根，创建 API、store、router、UI 与页面控制器

- `frontend/src/product-app.js`
  - 兼容启动协议的薄装配层，不承载业务

- `frontend/src/api/`
  - 领域 API；网络请求只能通过 `api/client.js`

- `frontend/src/state/` 与 `frontend/src/router.js`
  - 深层不可变状态
  - 管理 `page/project/table/left/right/focus` 深链和历史恢复

- `frontend/src/pages/`
  - 页面生命周期 adapter 与领域运行时
  - 每个 DOM 业务事件必须有唯一所有者

- `frontend/src/demo/mock-data.js`
  - 仅在用户显式切换演示模式后读取的种子数据

原则：

- 真实模式下不得自动回填 mock 结果冒充真实数据
- 页面和运行时只能调用领域 API，不得直接 `fetch` 或绕过 client
- `product-app.js`、`app.js` 和 runtime engine 必须保持薄装配层
- 禁止恢复 `window.DFI_UI`、document 自定义页面事件或隐式 DOM click 通信
- Live 与 Demo 必须使用不同 store，切换项目或模式时原子清理派生状态
- 所有订阅、事件绑定、定时器和页面控制器必须提供 cleanup
- 项目请求必须支持取消或 request generation，旧响应不得覆盖新项目
- 页面业务模块保持可审查规模，单文件不得演变为新的巨型控制器

## 4. 接口契约要求

### 4.1 错误返回

后端接口应尽量保持统一结构：

```json
{
  "error": "...",
  "detail": "...",
  "status_code": 4xx/5xx,
  "request_id": "..."
}
```

前端必须优先展示真实错误信息，不允许吞错后只显示泛化提示。

### 4.2 真实模式要求

以下页面在真实模式下必须基于 API 返回渲染，而不是仅弹 toast：

- 数据资产
- 血缘分析
- 版本比较
- 影响分析
- 导入历史 / 诊断

### 4.3 比较与影响分析

版本比较至少要能表达：

- 表新增 / 删除 / 变更
- 字段新增 / 删除 / 类型或语义变化
- 血缘边新增 / 删除
- 指标新增 / 删除 / 口径变化
- 风险变化
- 受影响 ADS 或下游对象摘要

## 5. 前端产品要求

### 5.1 数据资产页

数据资产页应优先支持：

- 按项目隔离查看
- 按加工层级筛选
- 按业务线 / 链路筛选
- 风险筛选
- 展示表级关键信息：
  - 层级
  - 分区
  - 粒度
  - 上游
  - ETL / DDL
  - 负责人
  - 更新频率
  - 备注

### 5.2 表详情页

表详情页的目标是“彻底真实化”，即：

- 表头信息来自真实选中表，而不是写死的示例表
- 字段列表来自真实字段集合
- 字段来源、加工逻辑、角色标签来自真实解析结果
- 上下游、加工证据、风险提示与版本信息来自真实接口

如果页面仍固定写着某张示例表名、示例 SQL、示例风险文案，就不算完成真实化。

### 5.3 演示模式

演示模式只能：

- 用户显式点击进入
- 清晰标注为演示
- 不发起真实写入

## 6. 测试与回归要求

### 6.1 改动后必须做的最低验证

所有代码改动至少执行：

```bash
make check
```

#### 后端改动

至少验证：

- `GET /api/health`
- 受影响接口 HTTP 状态
- JSON 是否可解析
- 错误返回是否符合约定

#### 前端改动

至少验证：

- 页面可加载
- 控制台无明显脚本错误
- 关键按钮不是“只有 toast 没有数据更新”
- 真实模式能拿到并渲染对应接口结果

### 6.2 关键链路回归

若改动涉及以下任一模块，必须做浏览器真回归：

- 导入向导
- 单表导入
- 数据资产页
- 表详情页
- 血缘分析页
- 版本比较页
- 影响分析页

若改动影响导入、比较、影响分析、路由/store 或跨层接口契约，还必须执行：

```bash
make dev
make p2-check
make stop
```

### 6.3 回归环境

- 前端：`15173`
- 后端：`18080`
- 不使用 `8080`

## 7. 改动原则

- 优先修真实链路，不优先堆视觉壳
- 优先消除“静态假数据覆盖真实接口”的问题
- 优先补结构化返回，不优先补表面文案
- P0-P2 已完成，后续默认小步修改，避免再次无验收地整体重构
- 未满足 `docs/future-roadmap.md` 的触发条件时，不主动实施完整 P3

若发现已有页面是静态示例壳：

- 可以逐步替换
- 但不能在真实模式下继续保留误导性的固定内容

## 8. 文档与交付

### 8.1 仓库内文档

功能、输入格式、验收说明优先更新：

- `README.md`
- `backend/README.md`
- `frontend/README.md`
- `docs/` 下相关文档

当前状态或范围变化时，必须同步更新根 `README.md`；架构职责或交付门禁变化时，必须同步更新本文件。

### 8.2 不应提交的内容

除非明确需要，不要提交：

- 运行日志
- 本地数据库
- 浏览器下载缓存
- 临时验证脚本
- 本地依赖目录（如 `.vendor_py/`、`.pw-browsers/`）

### 8.3 交付说明

提交结果时应说明：

- 改了哪些文件
- 接通了哪些真实链路
- 做了哪些验证
- 还有哪些页面仍是半静态或待真实化

## 9. 当前已知重点债务

以下内容应持续收口：

- 表详情页仍需彻底真实化
- 字段级编辑 / 批量保存 / 提交前 diff 还未闭环
- 版本比较页还应继续增强“证据 + 影响联动”
- 作业流、血缘、数据资产之间仍需更强跳转联动

## 10. 决策优先级

做取舍时按以下顺序：

1. 不破坏真实模式
2. 不误导用户
3. 先打通数据链路
4. 再做页面丰富度
5. 最后处理演示外观细节
