# 脱敏数据样例（可选）

每张表可放置一个 UTF-8 CSV，建议 20～100 行。文件名最好与表名对应。

样例仅用于辅助识别字段类型、基数、空值率和时间范围，不是血缘分析的必需输入。

上传前必须脱敏或删除：

- 姓名、手机号、邮箱、证件号；
- 企业名称、租户真实名称、内部 IP；
- Token、密钥、密码、Cookie 和连接串；
- 完整请求正文、错误详情及其他敏感业务内容。

可以保留字段结构、数据类型、枚举数量、时间先后关系和数值范围特征。
